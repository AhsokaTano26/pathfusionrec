[project]
name = "pathfusionrec"
version = "0.1.0"
description = "Structured semantic-interaction fusion for generative recommendation"
requires-python = ">=3.10"
dependencies = [
    "scikit-learn>=1.3",
    "sentence-transformers>=3.0.1",
    "torch>=2.0",
    "transformers>=4.41",
]
