name: pathfusionrec
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.12
  - pip
  - pytorch=2.5.1
  - pytorch-cuda=12.1
  - scikit-learn>=1.3
  - pip:
      - sentence-transformers>=3.0.1
      - transformers>=4.41
      - -e .
