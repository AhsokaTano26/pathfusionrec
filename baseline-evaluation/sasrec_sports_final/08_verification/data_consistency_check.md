# 数据一致性核验

已完成以下检查：

1. `all_item_seqs.json`：35,598名用户，交互序列总长度296,337；
2. 全部用户序列长度至少为5；
3. `id_mapping.json` 中的映射可将处理后序列转换成SASRec整数ID；
4. 转换得到的296,337条记录与 `sports.txt` 逐行完全一致；
5. 三个种子保存的数据统计完全相同；
6. 三个处理后文件的SHA256已写入 `04_data_provenance/processed_data_sha256.txt`。

该检查证明交付包内处理后数据彼此一致，不等同于证明原始数据到处理后序列的未归档预处理代码可以完全重放。

