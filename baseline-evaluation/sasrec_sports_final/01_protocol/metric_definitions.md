# 指标定义

- HR@K：测试正样本是否出现在前K个推荐结果中，对全部用户取平均。
- Recall@K：召回的测试正样本数除以测试正样本总数，再对用户汇总。
- NDCG@K：若正样本位于前K名，则按其排名给予对数折扣增益，再对用户取平均。

每位用户只有一个测试正样本，因此本实验中 Recall@K 与 HR@K 数值完全相同。

`candidate_item_count=18357` 表示模型对完整目录物品打分；各用户的训练与验证历史物品在打分后被屏蔽，所以它不是历史过滤后的实际剩余候选数。

